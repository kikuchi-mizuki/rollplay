# 進捗レポート - 2026年1月4日（セッション44）

## 📋 セッション概要

**日時**: 2026年1月4日
**セッション**: 44
**主な作業**: AI音声録音問題の根本原因特定と3段階の対策実装

---

## 🎯 作業内容

### 1. AnalyserNodeによる波形確認機能の実装

#### 実装内容

**ファイル**: `src/RoleplayApp.tsx`

- AnalyserNodeをWeb Audio API経路に追加
- Web Audio API接続構成: `ミキサーGain → AnalyserNode → MediaStreamDestination`
- AI音声再生時に波形データをリアルタイム確認
  - 100ms後と500ms後に波形データを取得
  - 振幅（amplitude）を計算して音声の有無を判定
  - 周波数データも取得

**実装コード**:
```typescript
// AnalyserNodeを作成してMediaStreamDestinationの前に挿入
const analyser = audioContext.createAnalyser();
analyser.fftSize = 2048;
analyser.smoothingTimeConstant = 0.8;
analyserNodeRef.current = analyser;

// ミキサー → AnalyserNode → MediaStreamDestination の順に接続
mixerGain.connect(analyser);
analyser.connect(recordingDestination);
```

**波形確認ログ**:
```typescript
// 音声再生開始から100ms後に波形データを取得
setTimeout(() => {
  analyser.getByteTimeDomainData(dataArray);

  // 波形データの統計情報を計算
  let min = 255, max = 0, sum = 0;
  for (let i = 0; i < bufferLength; i++) {
    const value = dataArray[i];
    if (value < min) min = value;
    if (value > max) max = value;
    sum += value;
  }
  const avg = sum / bufferLength;
  const amplitude = max - min;

  console.log('📊 [AnalyserNode] AI音声波形データ (100ms後):');
  console.log(`   - 振幅: ${amplitude} (最小=${min}, 最大=${max})`);
  console.log(`   - データ判定: ${amplitude > 10 ? '✅ 音声データあり' : '❌ 音声データなし'}`);
}, 100);
```

#### 結果

✅ **AI音声がMediaStreamDestinationに到達していることを確認**

```
📊 [AnalyserNode] AI音声波形データ (100ms後):
   - 振幅: 71 (最小=84, 最大=155)
   - 平均値: 128.80
   - データ判定: ✅ 音声データあり
   - 周波数最大値: 204

📊 [AnalyserNode] AI音声波形データ (500ms後): 振幅=105 ✅ 継続中
```

---

### 2. MediaRecorder詳細ログの追加

#### 実装内容

**ファイル**: `src/hooks/useRecording.ts`

MediaRecorderの全イベントに詳細ログを追加：

1. **ondataavailable**: チャンクごとのデータサイズとタイプ
```typescript
let dataEventCount = 0;
mediaRecorder.ondataavailable = (event: BlobEvent) => {
  dataEventCount++;
  if (event.data && event.data.size > 0) {
    chunksRef.current.push(event.data);
    console.log(`📦 [MediaRecorder] ondataavailable #${dataEventCount}:`);
    console.log(`   - データサイズ: ${event.data.size} bytes (${(event.data.size / 1024).toFixed(2)} KB)`);
    console.log(`   - データタイプ: ${event.data.type}`);
    console.log(`   - 累積チャンク数: ${chunksRef.current.length}`);
    console.log(`   - 累積データサイズ: ${chunksRef.current.reduce((sum, chunk) => sum + chunk.size, 0)} bytes`);
  }
};
```

2. **onstop**: 全チャンクの詳細とBlobサイズ
```typescript
mediaRecorder.onstop = () => {
  console.log('🛑 [MediaRecorder] onstop - 録画停止');

  // チャンクごとのサイズを表示
  console.log(`📊 [MediaRecorder] チャンク詳細:`);
  console.log(`   - チャンク数: ${chunksRef.current.length}`);
  chunksRef.current.forEach((chunk, index) => {
    console.log(`   - チャンク[${index}]: ${chunk.size} bytes (${chunk.type})`);
  });

  const totalSize = chunksRef.current.reduce((sum, chunk) => sum + chunk.size, 0);
  console.log(`   - 総データサイズ: ${totalSize} bytes (${(totalSize / 1024).toFixed(2)} KB)`);
};
```

3. **onstart/onpause/onresume**: 状態変化のログ
```typescript
mediaRecorder.onstart = () => {
  console.log('▶️ [MediaRecorder] onstart - 録画開始イベント');
  console.log(`   - state: ${mediaRecorder.state}`);
};
```

#### 結果

✅ **MediaRecorderは正常にデータを受信していることを確認**

```
📦 [MediaRecorder] ondataavailable #13:
   - データサイズ: 258619 bytes (252.56 KB)
   - データタイプ: video/webm;codecs=vp9,opus
   - 累積チャンク数: 13
   - 累積データサイズ: 3293086 bytes
```

---

### 3. RecordRTCライブラリの導入

#### 実装内容

**インストール**:
```bash
npm install recordrtc
npm install --save-dev @types/recordrtc
```

**実装**: `src/hooks/useRecording.ts`

MediaRecorderの代替としてRecordRTCを使用：

```typescript
const useRecordRTC = useRef<boolean>(true); // RecordRTCを使用するかどうか

// RecordRTCを使用する場合
if (useRecordRTC.current) {
  console.log('🎬 [RecordRTC] 録画開始...');

  const recorder = new RecordRTC(recordingStream, {
    type: 'video',
    mimeType: 'video/webm;codecs=vp9' as any,
    recorderType: RecordRTC.MediaStreamRecorder,
    audioBitsPerSecond: 128000,
    videoBitsPerSecond: 2500000,
    timeSlice: 1000,
    ondataavailable: (blob: Blob) => {
      chunksRef.current.push(blob);
      console.log(`📦 [RecordRTC] ondataavailable:`);
      console.log(`   - データサイズ: ${blob.size} bytes`);
    },
  });

  recordRTCRef.current = recorder;
  recorder.startRecording();
}
```

**停止処理**:
```typescript
if (recordRTCRef.current && isRecording) {
  console.log('🛑 [RecordRTC] 録画停止中...');

  recordRTCRef.current.stopRecording(() => {
    const blob = recordRTCRef.current!.getBlob();
    const duration = recordingTimeRef.current;

    console.log('✅ [RecordRTC] 録画停止完了');
    console.log(`   - 録画時間: ${duration}秒`);
    console.log(`   - Blobサイズ: ${(blob.size / 1024 / 1024).toFixed(2)} MB`);

    setRecordingData({ blob, duration, timestamp: new Date() });

    recordRTCRef.current!.destroy();
    recordRTCRef.current = null;
  });
}
```

#### 結果

✅ **RecordRTCは正常に動作**

```
🎬 [RecordRTC] 録画開始...
RecordRTC version: 5.6.2
started recording video stream.
Using recorderType: Q

✅ [RecordRTC] 録画停止完了
   - 録画時間: 18秒
   - Blobサイズ: 4.26 MB
   - Blobタイプ: video/webm;codecs=vp9,opus
```

❌ **しかし、録画ファイルにAI音声が含まれていない**

---

### 4. 音声トラックの手動ミキシング実装

#### 問題の特定

**根本原因**: ChromeのWebM/Opusエンコーダーは、Web Audio APIで動的に生成された`MediaStreamAudioDestinationNode`からの音声トラックを正しくエンコードできない

**確認済みの事実**:
1. ✅ Web Audio API: 正常動作（スピーカーから音声が聞こえる）
2. ✅ AnalyserNode: AI音声データを検出（振幅71、周波数204）
3. ✅ MediaStreamDestination: active状態、音声トラック存在
4. ✅ MediaRecorder: 2つの音声トラックを認識
5. ✅ RecordRTC: 2つの音声トラックを認識
6. ❌ **録画ファイル: AI音声が含まれていない**

#### 解決策

**従来の方法（問題あり）**:
```
カメラマイク（トラック1） ─┐
                          ├→ MediaRecorder → ❌ AI音声なし
AI音声（トラック2）      ─┘
```

**新しい方法（手動ミキシング）**:
```
カメラマイク ─→ MediaStreamSource ─┐
                                   ├→ GainNode → MediaStreamDestination → ミックス済みトラック
AI音声 ─────→ MediaStreamSource ─┘                                        ↓
                                                                     MediaRecorder
```

#### 実装内容

**ファイル**: `src/hooks/useRecording.ts` - `createCameraOnlyCompositeStream`関数

```typescript
// 音声トラックの手動ミキシング
console.log('🎵 音声トラックの手動ミキシング開始...');

// AudioContextを作成（音声ミキシング用）
const mixerContext = new AudioContext();

// ミキサーGainNodeを作成
const mixerGain = mixerContext.createGain();
mixerGain.gain.value = 1.0;

// MediaStreamDestinationを作成（ミックス後の音声出力先）
const mixedDestination = mixerContext.createMediaStreamDestination();
mixerGain.connect(mixedDestination);

let cameraSourceConnected = false;
let aiSourceConnected = false;

// カメラの音声をミキサーに接続
const cameraAudioTracks = cameraStream.getAudioTracks();
if (cameraAudioTracks.length > 0) {
  try {
    const cameraAudioStream = new MediaStream(cameraAudioTracks.map(t => t.clone()));
    const cameraSource = mixerContext.createMediaStreamSource(cameraAudioStream);
    cameraSource.connect(mixerGain);
    cameraSourceConnected = true;
    console.log('  ✅ カメラ音声をミキサーに接続');
  } catch (err) {
    console.warn('  ⚠️ カメラ音声のミキサー接続失敗:', err);
  }
}

// AI音声をミキサーに接続
const aiStream = audioDestinationRef?.current?.stream || aiAudioStream;
if (aiStream) {
  const aiAudioTracks = aiStream.getAudioTracks();
  if (aiAudioTracks.length > 0) {
    try {
      const aiSource = mixerContext.createMediaStreamSource(aiStream);
      aiSource.connect(mixerGain);
      aiSourceConnected = true;
      console.log('  ✅ AI音声をミキサーに接続');
    } catch (err) {
      console.warn('  ⚠️ AI音声のミキサー接続失敗:', err);
    }
  }
}

console.log(`🎵 音声ミキシング完了:`);
console.log(`   - カメラ音声: ${cameraSourceConnected ? '✅' : '❌'}`);
console.log(`   - AI音声: ${aiSourceConnected ? '✅' : '❌'}`);
console.log(`   - ミックス後の音声トラック数: ${mixedDestination.stream.getAudioTracks().length}`);

// ミックス後の音声トラックを合成ストリームに追加
const mixedAudioTracks = mixedDestination.stream.getAudioTracks();
mixedAudioTracks.forEach(track => {
  track.enabled = true;
  compositeStream.addTrack(track);
  console.log(`  🎵 ミックス済み音声トラック追加: enabled=${track.enabled}, readyState=${track.readyState}`);
});
```

#### 期待される効果

1. **MediaRecorderは1つのミックス済み音声トラックのみを処理**
   - 複数トラックのエンコード問題を回避

2. **Web Audio APIで完全にミックス**
   - カメラマイクとAI音声が物理的に1つの音声ストリームに統合
   - MediaRecorderは通常の音声トラックとして処理

3. **録画ファイルにAI音声が含まれる可能性が極めて高い**
   - Web Audio APIで生成した音声もエンコードされることを期待

---

## 📁 変更ファイル一覧

### フロントエンド (TypeScript/React)

1. **`src/RoleplayApp.tsx`**
   - AnalyserNodeのRef追加
   - Web Audio API初期化時にAnalyserNode追加
   - AI音声再生時に波形データ確認ログ追加

2. **`src/hooks/useRecording.ts`**
   - RecordRTCライブラリのインポート
   - RecordRTCレコーダーのRef追加
   - RecordRTC使用フラグ追加（`useRecordRTC`）
   - RecordRTC録画開始・停止処理の実装
   - MediaRecorder詳細ログの追加（ondataavailable、onstop、onstart等）
   - 音声トラックの手動ミキシング機能の実装

3. **`package.json`**
   - recordrtc依存関係の追加
   - @types/recordrtc依存関係の追加（devDependencies）

---

## 🔄 Gitコミット履歴

```
1b81f28 feat: 音声トラックの手動ミキシング実装（AI音声録音問題の根本対策）
d438df5 feat: RecordRTCライブラリを導入してMediaRecorderの代替実装を追加
44a7ba6 feat: AnalyserNodeとMediaRecorder詳細ログを追加（AI音声録音問題の調査強化）
```

---

## 🔍 技術的な分析

### セッション44で判明した事実

#### 正常に動作している部分

1. ✅ **Web Audio API**: 完全に正常動作
   - スピーカーからAI音声が正常に再生される
   - AudioContext、GainNode、MediaStreamDestinationすべて成功

2. ✅ **AnalyserNode**: AI音声データを明確に検出
   - 振幅: 71-105（明確な音声信号）
   - 周波数最大値: 204
   - データ判定: ✅ 音声データあり

3. ✅ **MediaStreamDestination**: 完全に正常
   - stream.active = true
   - 音声トラック数: 1
   - AI音声が確実にストリームに流れている

4. ✅ **MediaRecorder**: トラックを認識
   - 2つの音声トラック（カメラ + AI）を認識
   - 各トラックは enabled=true, readyState=live
   - データを正常に受信（約250-320KB/秒）

5. ✅ **RecordRTC**: トラックを認識
   - MediaRecorderと同様に2つの音声トラックを認識
   - データを正常に受信

#### 問題点

❌ **録画ファイルにAI音声が含まれない**

**根本原因の特定**:
- ブラウザ（Chrome）のWebM/Opusエンコーダーの制限
- Web Audio APIで動的に生成された`MediaStreamAudioDestinationNode`からの音声トラックを正しくエンコードできない
- MediaRecorderは音声トラックを「認識」するが、実際のエンコーディング時に無視される

### Web Audio API音声経路

**セッション43まで（2つの音声トラック）**:
```
[AudioBufferSource]
     ↓
[GainNode (×4)]  ← AI音声を4倍に増幅
     ↓
[aiMixerGainNode (×1)]  ← ミキサー
     ├→ [AudioContext.destination]  ← スピーカー出力（✅ 正常動作）
     └→ [AnalyserNode]  ← 波形確認（✅ 音声データ検出）
          ↓
     [MediaStreamDestination]  ← AI音声トラック生成
          ↓
     [MediaRecorder] ← カメラマイク（トラック1）+ AI音声（トラック2）
          ↓
     ❌ AI音声が録画ファイルに含まれない
```

**セッション44（手動ミキシング）**:
```
カメラマイク ─→ [MediaStreamSource] ─┐
                                      ├→ [GainNode] → [MediaStreamDestination] → ミックス済みトラック
AI音声 ─────→ [MediaStreamSource] ─┘                                              ↓
                                                                            [MediaRecorder]
                                                                                 ↓
                                                                    期待: ✅ AI音声が含まれる
```

---

## 🚧 次のステップ

### 優先度1: 手動ミキシングの検証

1. **録画ファイルの確認**
   - ダウンロードした録画ファイルを再生
   - AI音声が含まれているか確認
   - 音質の確認

2. **ログの確認**
   ```
   🎵 音声トラックの手動ミキシング開始...
     ✅ カメラ音声をミキサーに接続
     ✅ AI音声をミキサーに接続
   🎵 音声ミキシング完了:
      - カメラ音声: ✅
      - AI音声: ✅
      - ミックス後の音声トラック数: 1
   ```

### 優先度2: 手動ミキシングが失敗した場合の代替案

もし手動ミキシングでもAI音声が録音されない場合：

1. **バックエンド録音方式**
   - TTS音声生成時にバックエンドで録音
   - フロントエンドに音声ファイルとして送信
   - 録画終了後に映像と音声を合成

2. **クライアントサイド事後合成**
   - 録画中にAI音声を別トラックで保存
   - 録画終了後にFFmpeg.wasmで映像と音声を合成

3. **ブラウザの変更**
   - Firefoxなど別のブラウザでの動作確認
   - Safari（WebKit）での動作確認

---

## 📝 技術的なメモ

### セッション44で試した対策の総まとめ

| 対策 | 実装 | 結果 | 原因分析 |
|------|------|------|----------|
| AnalyserNode波形確認 | ✅ | AI音声検出 ✅ | Web Audio APIは正常 |
| MediaRecorder詳細ログ | ✅ | データ受信 ✅ | トラック認識は正常 |
| RecordRTC代替実装 | ✅ | トラック認識 ✅ | エンコーダー問題 |
| 音声手動ミキシング | ✅ | **検証待ち** | **根本対策の可能性** |

### セッション40-44の累積対策

1. ✅ MediaStreamDestinationトラック直接使用
2. ✅ GainNodeミキサー常時接続方式
3. ✅ Oscillator連続信号でアクティブ化
4. ✅ 音量増幅（2倍 → 4倍）
5. ✅ GainNode設定方法の修正（setValueAtTime → 直接代入）
6. ✅ 詳細なデバッグログ
7. ✅ AnalyserNodeで波形確認
8. ✅ MediaRecorder詳細ログ
9. ✅ RecordRTC代替実装
10. ✅ **音声トラックの手動ミキシング（NEW）**

---

## ⚙️ 環境情報

- **ブラウザ**: Chrome（最新版）
- **OS**: macOS
- **Web Audio API**: 正常動作
- **MediaRecorder API**: 正常動作（トラック認識OK、エンコード問題）
- **RecordRTC**: 正常動作（バージョン 5.6.2）
- **MediaStreamDestination**: active状態
- **AnalyserNode**: AI音声検出成功

---

**レポート作成日時**: 2026年1月4日
**次回セッション**: セッション45（日時未定）
**次回の焦点**: 手動ミキシングの検証結果に基づいた次のアクション決定
